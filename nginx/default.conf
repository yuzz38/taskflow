upstream taskflow_backend {
    server backend:3000;
}

server {
    listen 80;
    server_name localhost;

    # Логи доступа и ошибок
    access_log /var/log/nginx/taskflow_access.log;
    error_log  /var/log/nginx/taskflow_error.log;

    # Сжатие для ускорения отдачи статики
    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
    gzip_min_length 256;

    # --- Клиентский уровень: статика фронтенда отдаётся самим nginx ---
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # --- Серверный уровень: все /api/* проксируются в контейнер backend ---
    location /api/ {
        proxy_pass http://taskflow_backend;

        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";

        proxy_connect_timeout 10s;
        proxy_read_timeout    30s;
    }
}
